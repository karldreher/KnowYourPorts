
name: development-container-scan

on:
  push:
    branches: 
      - development

jobs:
  scan:
    runs-on: ubuntu-latest

    - name: Container image scan
      uses: Azure/container-scan@v0
      with:
         image-name: karldreher/knowyourports:development
